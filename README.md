# QuestForge 🔮
### System Design Interviews, Gamified

![HTML5](https://img.shields.io/badge/HTML5-E34F26?style=flat&logo=html5&logoColor=white)
![CSS3](https://img.shields.io/badge/CSS3-1572B6?style=flat&logo=css3&logoColor=white)
![JavaScript](https://img.shields.io/badge/JavaScript-F7DF1E?style=flat&logo=javascript&logoColor=black)
![Gemini API](https://img.shields.io/badge/Google_Gemini-1.5_Flash-4285F4?style=flat&logo=google&logoColor=white)
![Pollinations.ai](https://img.shields.io/badge/Pollinations.ai-Free_AI_Images-blueviolet?style=flat)
![GitHub Pages](https://img.shields.io/badge/GitHub_Pages-Live-brightgreen?style=flat&logo=github)
![Mobile Friendly](https://img.shields.io/badge/Responsive-Mobile_%7C_Tablet_%7C_Desktop-success?style=flat)

🎮 **[Live Demo → mayageva11.github.io/questforge](https://mayageva11.github.io/questforge)**

---

![QuestForge Demo — AI-generated pixel-art quest scene](assets/demo-quest.png)
*AI-generated pixel-art quest scene — created by Pollinations.ai inside the app*

---

## What is QuestForge?

QuestForge turns dry **System Design interview questions** into retro fantasy RPG quests.

You pick a challenge (or type your own). The app calls Google Gemini to generate:
- ⚔️ A **quest title** in the style of a game level
- 📜 A **fantasy narrative** that maps system design concepts to fantasy elements
- 💡 A **real architectural lesson** hidden inside the story
- 🎨 A **pixel-art scene** — AI-generated by Pollinations.ai, no API key needed

Every quest teaches you something real about system design — but feels like playing a game.

---

## Why I built this

While preparing for **Senior QA / System Design interviews**, I kept hitting the same wall: design questions felt abstract and intimidating. I couldn't make them stick by just reading docs.

So I built QuestForge. Instead of grinding another article, I translate every challenge into an RPG quest — because learning sticks better when it doesn't feel like studying.

**Built by Maya Erusalimsky during the great job hunt of 2025–2026. Shipped because why not.**

---

## Demo — What a Quest Looks Like

> **"Design a Rate Limiter for a High-Traffic API"**

The app generates something like:

> **⚔️ The Gatekeeper's Toll: Siege of the Iron Gate**
>
> *"At the Iron Gate of the Kingdom's API, the Gatekeeper Troll stands watch. Chaos Wraiths — unbounded traffic bursts — threaten to overrun the fortress. The Twin Sorcerers of sliding windows debate whether to count requests by second or by minute; one is precise, the other is kind. The Scroll Vaults hold the token buckets: refill them too slowly and loyal knights wait in vain; refill too fast and the Wraiths flood the gate. The Troll's decision — fixed window, sliding log, or token bucket — will determine who enters the Kingdom tonight.*"
>
> 💡 **Lesson:** Choose token bucket for bursty-traffic APIs — it allows short bursts while enforcing sustained rate limits, unlike fixed windows that cause thundering-herd resets at window boundaries.

---

## How the AI Works

### Google Gemini 1.5 Flash — Text Generation

QuestForge uses the **Google Gemini 1.5 Flash** model for all quest narrative generation.

```
GET/POST https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=YOUR_KEY
```

**Why Gemini?**

| Reason | Detail |
|--------|--------|
| ✅ **Free tier** | 15 requests/min, 1,000,000 tokens/day — enough for real use |
| ✅ **No billing required** | Anyone can get a Gemini key from [Google AI Studio](https://aistudio.google.com) in 2 minutes, no credit card |
| ✅ **Fast** | Flash model is optimized for low latency — quests generate in ~2 seconds |
| ✅ **JSON output** | Reliable structured output with a well-crafted prompt |

The prompt instructs Gemini to map system design concepts to fantasy metaphors:

```
Rate Limiter     → The Gatekeeper's Toll
Load Balancer    → The Twin Sorcerers
Cache            → The Crystal Memory Vault
Database         → The Ancient Tome of Records
Message Queue    → The Messenger Ravens
Docker Container → The Summoning Circle
CDN              → The Network of Mirror Portals
Circuit Breaker  → The Failsafe Seal
```

Gemini returns structured JSON with `title`, `story`, and `lesson` — parsed directly in the browser.

### Pollinations.ai — Pixel-Art Image Generation

The **quest illustration** is generated by [Pollinations.ai](https://pollinations.ai) — a completely free, no-key image generation API.

```js
const url = `https://image.pollinations.ai/prompt/${encodedPrompt}?width=832&height=448&model=flux&nologo=true&seed=${Date.now()}`;
```

No API key. No billing. The URL itself is the request. The image loads directly into the `<img>` tag.

The prompt is built from the Gemini quest output, requesting a **16-bit pixel-art RPG scene** with dark purple backgrounds and neon glow effects — matching the app's visual style perfectly.

---

## The Journey: Claude → Gemini

QuestForge **started with the Anthropic Claude API** (`claude-sonnet-4-6`). Claude was the original choice because of its strong narrative reasoning and structured JSON output.

**Why the switch to Gemini?**

Claude's API requires a paid credit balance — there's no free tier for browser-based calls. After the initial balance ran out, the app returned:

```
Error 400: Your credit balance is too low to access the Claude API.
```

Gemini 1.5 Flash has a genuine, unlimited free tier — 15 RPM and 1M tokens/day — with no credit card required. For a portfolio project you want *anyone* to try, Gemini is the right call.

The architecture stayed identical — both APIs accept a prompt and return text. Only the endpoint, headers, and key format changed.

---

## How the Project Was Deployed

This entire project was built and deployed using **Claude Code** (Anthropic's AI coding assistant) without ever touching the GitHub web UI.

**Steps taken, all via terminal/API:**

1. **Created the GitHub repository** using the GitHub REST API:
   ```bash
   curl -X POST https://api.github.com/user/repos \
     -H "Authorization: token $GITHUB_TOKEN" \
     -d '{"name":"questforge","private":false}'
   ```

2. **Enabled GitHub Pages** via the API:
   ```bash
   curl -X POST https://api.github.com/repos/mayageva11/questforge/pages \
     -H "Authorization: token $GITHUB_TOKEN" \
     -d '{"source":{"branch":"main","path":"/"}}'
   ```

3. **Pushed the code** using git with inline credential helper (never stored the token in `.git/config`)

4. **Live in ~2 minutes** at `https://mayageva11.github.io/questforge`

Claude Code acted as a **GitHub automation layer** — what some call "GitHub MCP" (Model Context Protocol integration) — orchestrating the entire deployment pipeline from prompt to live URL without manual browser clicks.

---

## Features

- 🎮 **8 system design quick-pick challenges** — one click to load
- ✏️ **Custom challenge input** — type any design question
- 🤖 **Gemini 1.5 Flash** — generates narrative + lesson in ~2 seconds
- 🎨 **Pollinations.ai** — free AI pixel-art scene, unique every time
- 📋 **Copy to LinkedIn** — one-click formatted post with the quest + lesson + your link
- 🔄 **New Quest** — instant reset without page reload
- ⚙️ **Settings modal** — manage your API key (stored only in localStorage, never sent anywhere else)
- 📱 **Fully responsive** — mobile, tablet, and desktop layouts
- 🌐 **No backend, no build step** — pure HTML/CSS/JS, zero dependencies

---

## System Design Quick Quests

| # | Challenge |
|---|-----------|
| 1 | Design a URL shortener (like bit.ly) |
| 2 | Design a rate limiter for a high-traffic API |
| 3 | Design a distributed cache system |
| 4 | How would you design Twitter's news feed? |
| 5 | Design a notification system at scale |
| 6 | Design a real-time collaborative document editor |
| 7 | How would you design a video streaming platform? |
| 8 | Design a payment processing system |

---

## Tech Stack

| Layer | Technology |
|-------|------------|
| Frontend | Vanilla HTML5 + CSS3 + JavaScript (ES2022) |
| Fonts | Plus Jakarta Sans · Outfit · JetBrains Mono (Google Fonts) |
| AI — Text | Google Gemini 1.5 Flash (free tier) |
| AI — Images | Pollinations.ai / Flux model (free, no key) |
| Animations | CSS View Transitions API + fallback |
| Storage | `localStorage` (API key only, never leaves your browser) |
| Hosting | GitHub Pages (static, zero cost) |
| Deployment | GitHub REST API via curl + Claude Code |

**Zero npm. Zero build. Zero backend. Open index.html and it works.**

---

## Local Setup

```bash
git clone https://github.com/mayageva11/questforge.git
cd questforge
open index.html   # macOS
# or just drag index.html into your browser
```

Then:
1. Get a **free** Gemini API key at [aistudio.google.com](https://aistudio.google.com) (takes ~2 min, no credit card)
2. Click the ⚙️ gear icon → paste your key → Save
3. Pick a challenge and hit **Forge Quest ⚔️**

Your API key is stored only in your browser's `localStorage`. It never touches any server.

---

## Deploy Your Own

1. **Fork** this repo
2. Go to **Settings → Pages**
3. Set source to **Deploy from branch → main → / (root)**
4. Wait ~2 minutes → your copy is live at `https://YOUR_USERNAME.github.io/questforge`

No configuration needed. No environment variables. The user brings their own Gemini key.

---

## Design

Glassmorphic dark UI with a fantasy-RPG aesthetic:

- **Color palette:** Deep purple `#06040b` background, pink `#f472b6`, lavender `#a78bfa`, sky blue `#38bdf8`, amber `#fbbf24`
- **Glitch animation** on the app title — fires once on load
- **Shimmer skeleton** while the quest image loads
- **View Transitions API** for smooth screen-to-screen navigation (with fallback for Safari)
- **Safari-hardened** modal system — triple-layer hide (`display: none !important`, `visibility: hidden`, inline style) to prevent bleed-through

---

## Responsive Design

| Breakpoint | Layout |
|------------|--------|
| `> 860px` | 3-column challenge grid, full canvas |
| `640–860px` | 2-column grid, reduced padding |
| `< 640px` | Single column, row-layout cards, 52px touch targets |
| `< 380px` | Very small phone tweaks |

---

*Built by Maya Erusalimsky · [github.com/mayageva11](https://github.com/mayageva11) · 2025–2026*
